function getResult(score) {
    let score = Number;
    const nama = "";
    if (score >= 80 && score <=100) {
        let nilai = "Grade A"
        console.log(nama + "mendapatkan" + nilai)
    }
}

getResult(85);